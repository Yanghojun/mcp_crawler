build:
  dockerfile: Dockerfile

startCommand:
  type: http
  configSchema:
    type: object
    required: []
    properties: {}
    description: No configuration required.
  commandFunction: |
    config => ({
      command: 'python',
      args: ['crawler.py'],
      env: {},
      port: 8000
    })