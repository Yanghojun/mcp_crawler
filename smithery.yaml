build:
  dockerfile: Dockerfile

startCommand:
  type: stdio
  configSchema:
    type: object
    required: []
    properties: {}
    description: No configuration required.
  commandFunction: |
    config => ({ command: 'python', args: ['crawler.py'] })